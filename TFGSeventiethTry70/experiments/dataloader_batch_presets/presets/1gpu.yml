# Preset 1×GPU — cluster/run_seventieth_try70_1gpu.slurm (6 CPU, 38G, gres=gpu:1).
# Copia a data: / training: del try70_expert_*.yaml.
#
# data:
num_workers: 0
val_num_workers: 0
persistent_workers: false
val_persistent_workers: false
prefetch_factor: 2
#
# training:
batch_size: 1
gradient_accumulation_steps: 16
