# Cluster Access — DAC UPC (sert)

**This file is gitignored. Never commit it.**

## SSH

```bash
ssh gmoreno@sert.ac.upc.edu
```

## Credentials to try

| Username | Password |
|---|---|
| `gmoreno` | `Tallypass+0504` |
| `gmoreno` | `Slenderman,2004` |
| `guillem.moreno.garcia` | `Tallypass+0504` |
| `guillem.moreno.garcia` | `Slenderman,2004` |

> Note: `gmoreno` is the Linux/SSH username. `guillem.moreno.garcia` is the SSO/web username only.
> Previous guide also listed `Mamareentanga54` (may be outdated).

## VPN

- Must connect **UPCLink** (Fortinet SSL VPN) before SSH works.
- VPN adapter: `Fortinet SSL VPN Virtual Ethernet Adapter`
- Cluster IP: `147.83.31.105`

## Paths on cluster

| Purpose | Path |
|---|---|
| venv | `/scratch/nas/g/gmoreno/tf_env/bin/activate` |
| NAS scratch | `/scratch/nas/g/gmoreno/` |
| Project dir | `/scratch/nas/g/gmoreno/TFGSecondTry2/` |

## SSH tool on this machine

```
C:\Program Files\Git\usr\bin\ssh.exe
C:\Program Files\Git\usr\bin\rsync.exe
```
