# Try 80 Code Line Count

Clean executable/project-code count for the final Try 80 project:

| Category | Lines |
|---|---:|
| Python source | 5,159 |
| SLURM scripts | 147 |
| YAML config | 56 |
| **Code/config total** | **5,362** |

This excludes calibration JSON assets, generated LaTeX build artifacts,
documentation-only files, caches, binaries, datasets, and model checkpoints.

The full text tree including calibration JSONs is much larger, mainly because
`try78_los_two_ray_calibration.json` contains the dense calibrated prior data.
