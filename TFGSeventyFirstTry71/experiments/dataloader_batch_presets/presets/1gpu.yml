# Preset 1×GPU — alinea con cluster/run_seventyfirst_try71_1gpu.slurm (6 CPU, 38G, gres=gpu:1).
# Copia bloques a data: / training: del try71_expert_*.yaml (ver README.md).
#
# data:
num_workers: 0
val_num_workers: 0
persistent_workers: false
val_persistent_workers: false
prefetch_factor: 2
#
# training:
batch_size: 1
gradient_accumulation_steps: 16
